name: Split

on:
  push:
    branches: [ master ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: Run
      env:
        TOKEN: ${{ secrets.TOKEN }}
      run: |
        sh shell/split-linux.sh
